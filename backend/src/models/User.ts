// Models are now managed via Prisma schema — see prisma/schema.prisma
// This file is kept for backward compatibility of imports but is no longer used.
// All database access goes through: import prisma from "../lib/prisma";
export {};
